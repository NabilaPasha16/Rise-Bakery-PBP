# 📊 GoRouter Implementation Diagram

## 🔄 Navigation Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    RISE BAKERY APP FLOW                         │
└─────────────────────────────────────────────────────────────────┘

                        START
                          ↓
                    ┌─────────────┐
                    │   Splash    │ (3 detik)
                    │   Screen    │
                    └──────┬──────┘
                           ↓
                    context.toLogin()
                           ↓
                    ┌─────────────┐
         ┌─────────→│   Login     │←─────────┐
         │          │   Page      │          │
         │          └──────┬──────┘          │
         │                 ↓                 │
    context.back()   ┌──────────────┐  context.toRegister()
         │           │ Login Valid? │          │
         │           └──────┬───────┘          │
         │                  ↓                  │
         │           ┌─────────────┐           │
         │           │   Home      │           │
         │    ←──────│   Page      │           │
         │           │ (email)     │           │
         │           └──────┬──────┘           │
         │                  ↓                  │
         │    ┌─────────────┴─────────────┐   │
         │    ↓             ↓             ↓   │
    context.back()  context.toCart()  context.toProfile()
         │    │             │             │   │
         │    ↓             ↓             ↓   │
         │ ┌────┐      ┌────────┐    ┌────────┐
         │ │Det-│      │ Cart   │    │Profile │
         │ │ail │      │ Page   │    │ Page   │
         │ └────┘      └────────┘    └────────┘
         │    ↓             ↓             ↓
         │ context.toCart() │   context.toLogin() → [Logout]
         │    │             ↓
         │    └────────→┌─────────┐
         │              │ Details │
         │              │ Confirm │
         └──────────────┘
```

---

## 🎯 Routes Structure

```
app_router.dart (Main Configuration)
│
├── /splash (SplashScreen)
│   └── 3 sec delay → /login
│
├── /login (LoginPage)
│   ├── Valid login → /home (with email)
│   ├── Register button → /register
│   └── Back ← /register
│
├── /register (RegisterPage)
│   ├── Success → /login
│   └── Back → /login
│
├── /home (HomePage)
│   ├── Email: required
│   ├── Cake item → /detail (with cake)
│   ├── Cart button → /cart
│   ├── Profile button → /profile (with email)
│   └── Back → previous page
│
├── /detail (DetailPage)
│   ├── Cake: required
│   ├── Back button → /home
│   ├── Buy now → /cart (buyNowItem)
│   └── Continue shopping → /home
│
├── /cart (CartPage)
│   ├── Items: optional
│   ├── BuyNowItem: optional
│   ├── Back → /home or /detail
│   ├── Continue shopping → previous page
│   └── Checkout → /home
│
├── /category (CategoryPage)
│   ├── Category: required (string)
│   ├── Cakes: required (List<Cake>)
│   ├── Cake item → /detail (with cake)
│   └── Back → /home
│
├── /profile (ProfilePage)
│   ├── Email: required
│   ├── Logout → /login
│   └── Back → /home
│
└── /api-cakes (ApiCakesPage)
    ├── Cake item → /detail (with cake)
    └── Back → /home
```

---

## 🔌 Component Interaction

```
┌──────────────────────────────────────────────────────────┐
│                     GOROUTER SYSTEM                      │
└──────────────────────────────────────────────────────────┘

User Interaction
        ↓
      Button/Gesture
        ↓
┌─────────────────────┐
│   Widget Context    │
│  (BuildContext)     │
└──────────┬──────────┘
           ↓
┌─────────────────────────────────────────────┐
│     NavigationExtension (navigation_      │
│          helpers.dart)                     │
│                                           │
│  • context.toHome()                      │
│  • context.toDetail(cake: cake)          │
│  • context.toCart()                      │
│  • context.back()                        │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│     AppNavigation Class                     │
│  (Static Methods)                           │
│                                           │
│  • AppNavigation.toHome(context)          │
│  • AppNavigation.toDetail(context, cake)  │
│  • AppNavigation.back(context)            │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│     GoRouter                                │
│  (via GoRouter.of(context))                │
│                                           │
│  • go() - Replace current route           │
│  • push() - Add to stack                  │
│  • pop() - Remove from stack              │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│  app_router.dart (Route Configuration)      │
│                                           │
│  • Finds matching route (/detail)         │
│  • Passes parameters (cake object)        │
│  • Builds correct widget (DetailPage)     │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│     Target Widget                           │
│  (DetailPage with cake data)                │
└─────────────────────────────────────────────┘
```

---

## 📦 Parameter Passing Flow

```
Source Page → Navigation → Router → Target Page

EXAMPLE: Navigasi ke DetailPage

┌──────────────┐
│  Home Page   │
│  (has cake   │
│   object)    │
└──────┬───────┘
       │
       │ context.toDetail(cake: myCake)
       ↓
┌─────────────────────────┐
│ navigation_helpers.dart │
│                        │
│ context.toDetail({     │
│   required Cake cake   │
│ })                     │
└──────┬──────────────────┘
       │ calls GoRouter.of(context).go(
       │   '/detail',
       │   extra: cake ← Object passed here
       │ )
       ↓
┌──────────────────────────┐
│   app_router.dart        │
│ (Route Definition)       │
│                         │
│ GoRoute(                │
│   path: '/detail',      │
│   builder: (context,    │
│      state) {           │
│     final cake =        │
│       state.extra as    │
│       Cake?;            │
│   }                     │
└──────┬─────────────────┘
       │
       │ cake object extracted
       ↓
┌──────────────────────────┐
│   Detail Page            │
│                         │
│ DetailPage(cake: cake)  │
│ receives the object     │
└──────────────────────────┘
```

---

## 🎨 File Architecture

```
lib/
│
├── router/
│   ├── app_router.dart
│   │   └── Defines all 9 routes
│   │       with builders
│   │
│   ├── navigation_helpers.dart
│   │   ├── AppNavigation class
│   │   ├── NavigationExtension
│   │   └── GoRouterExtensionHelper
│   │
│   ├── GO_ROUTER_GUIDE.md
│   │   └── Complete documentation
│   │
│   └── IMPLEMENTATION_EXAMPLES.dart
│       └── Code examples for each page
│
├── pages/
│   ├── splash_screen.dart ✅
│   ├── login_page.dart 🔄
│   ├── register_page.dart 🔄
│   ├── home_page.dart 🔄
│   ├── detail_page.dart 🔄
│   ├── cart_page.dart 🔄
│   ├── category_page.dart 🔄
│   ├── profile_page.dart 🔄
│   └── api_cakes_page.dart 🔄
│
├── main.dart ✅
│   └── Uses MaterialApp.router
│
└── Other directories...
```

---

## 🔀 State Transitions

```
STATE DIAGRAM:

┌─────────────────────────────────────────────────────────┐
│  Authentication State Machine                           │
└─────────────────────────────────────────────────────────┘

                    START
                      ↓
                  ┌────────┐
         ┌────────→│Splash  │←─┐
         │         └─┬──────┘  │
         │           │         │ No session
         │    3 sec  ↓         │
         │        ┌──────┐     │
         └────────│Login │─────┘
                  └──┬───┘
                     │
            ┌────────┴────────┐
            │                 │
            ↓                 ↓
      ┌─────────┐        ┌────────┐
      │Register │        │Invalid │
      └────┬────┘        └───────┘
           ↓
      ┌────────────┐
      │Success?    │
      └────┬───────┘
           ├─ Yes ─→ [Back to Login]
           │
           └─ No ──→ [Show error]
                     ↓
                  [Retry]


           ┌──────────────────────────────┐
           │  AUTHENTICATED STATE          │
           └──────────────────────────────┘
                      ↓
               ┌────────────────┐
         ┌────→│   Home Page    │←────┐
         │     └────────────────┘     │
         │            │               │
    Can navigate  View cakes    Can navigate
    anywhere      Browse        to:
    in app        Categories    • Detail
                  View Profile  • Cart
                  View API      • Profile
                  Cakes         • Category
                                • API Cakes
         │             │              │
         └─────────────┴──────────────┘
                      ↓
            ┌──────────────────┐
            │  Logout Triggered│
            └────────┬─────────┘
                     │
                     ↓
                ┌──────────────┐
                │ Clear Cache  │
                │ Clear Auth   │
                └─────┬────────┘
                      │
                      ↓
                  [Go to Login]
```

---

## 💾 Data Flow

```
PARAMETER PASSING EXAMPLE: Home → Detail → Cart

Home Page
  ├── cake object exists
  │   ├── id: 1
  │   ├── name: "Chocolate Cake"
  │   ├── price: 50000
  │   └── image: "assets/cake.jpg"
  │
  └── User taps cake
      │
      └─→ context.toDetail(cake: cake)
          │
          ├─→ GoRouter.of(context).go(
          │   '/detail',
          │   extra: cake ← whole object
          │ )
          │
          └─→ Navigates to DetailPage
              │
              └─→ DetailPage receives cake
                  │
                  └─→ Displays cake details
                      │
                      └─→ User taps "Buy Now"
                          │
                          └─→ context.toCart(
                              buyNowItem: widget.cake
                            )
                            │
                            ├─→ Goes to CartPage
                            │
                            └─→ CartPage shows
                                this item in cart
```

---

## 🎯 Error Handling

```
GoRouter Error Flow

┌────────────────────────┐
│  User navigates to     │
│  invalid/unknown route │
│  (e.g., /xyz)         │
└────────┬───────────────┘
         │
         ↓
┌────────────────────────┐
│  GoRouter catches      │
│  unmapped route        │
└────────┬───────────────┘
         │
         ↓
┌────────────────────────┐
│  Calls errorBuilder()  │
│  from app_router.dart  │
└────────┬───────────────┘
         │
         ↓
┌────────────────────────┐
│  Shows error page      │
│  with "Go to Home"     │
│  button                │
└────────┬───────────────┘
         │
         ↓
┌────────────────────────┐
│  User clicks button    │
│  Navigates to home     │
└────────────────────────┘
```

---

## 📈 Performance Optimization

```
GoRouter Benefits for Performance:

┌──────────────────────────────────────┐
│  OLD: Using Navigator.push          │
│  ❌ Need to create MaterialPageRoute │
│  ❌ Need to build widget every time  │
│  ❌ More memory usage                │
│  ❌ Slower navigation                │
└──────────────────────────────────────┘
                  ↓
         SLOW & MEMORY HEAVY


┌──────────────────────────────────────┐
│  NEW: Using GoRouter                 │
│  ✅ Route predefined in one place    │
│  ✅ Widget built on demand           │
│  ✅ Better memory management         │
│  ✅ Faster navigation                │
│  ✅ Automatic caching                │
└──────────────────────────────────────┘
                  ↓
         FAST & OPTIMIZED
```

---

**Diagram ini membantu memahami bagaimana GoRouter bekerja dalam Rise Bakery App! 🚀**
